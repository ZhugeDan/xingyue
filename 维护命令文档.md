# 日记应用维护命令文档

## 🚀 应用启动

### 开发模式
```bash
cd /home/diary-app/diary-app
npm run dev
```
- 启动开发服务器
- 默认端口：3001 (如果3000被占用)
- 自动热重载，文件修改后自动编译

### 生产模式
```bash
cd /home/diary-app/diary-app
npm run build
npm start
```
- 构建生产版本
- 启动生产服务器

## 🔍 状态检查

### 查看运行进程
```bash
ps aux | grep -E "(node|next|npm)" | grep -v grep
```
- 查看所有Node.js/Next.js相关进程

### 检查端口占用
```bash
netstat -tlnp | grep -E "(3000|3001)"
```
- 查看3000和3001端口的占用情况
- 确认应用运行端口

### 查看实时日志
```bash
# 进入项目目录
cd /home/diary-app/diary-app

# 开发模式日志
npm run dev

# 生产模式日志
npm start
```

## 🛠️ 重启服务

### 完全重启开发服务器
```bash
# 1. 停止当前服务 (Ctrl+C)
# 2. 清理缓存
npm run clean
# 或者手动删除
rm -rf .next node_modules/.cache

# 3. 重新安装依赖 (如有必要)
npm install

# 4. 重新启动
npm run dev
```

### 重启生产服务
```bash
# 1. 停止当前进程
pkill -f "next start"

# 2. 重新构建
npm run build

# 3. 启动服务
npm start
```

## 🧹 清理操作

### 清理Next.js缓存
```bash
cd /home/diary-app/diary-app
rm -rf .next
rm -rf node_modules/.cache
```

### 清理依赖
```bash
cd /home/diary-app/diary-app
rm -rf node_modules
rm package-lock.json
npm install
```

### 清理所有构建文件
```bash
cd /home/diary-app/diary-app
rm -rf .next
rm -rf out
rm -rf dist
```

## 🔧 端口管理

### 强制释放端口
```bash
# 查找占用端口的进程
lsof -ti:3000
lsof -ti:3001

# 终止进程
kill -9 $(lsof -ti:3000)
kill -9 $(lsof -ti:3001)
```

### 指定端口启动
```bash
# 使用环境变量指定端口
PORT=3002 npm run dev

# 或者修改package.json
# "scripts": { "dev": "next dev -p 3002" }
```

## 📊 性能监控

### 内存使用情况
```bash
ps aux | grep node | awk '{print $4, $11}' | sort -nr
```

### CPU使用情况
```bash
top -p $(pgrep -f "next")
```

### 磁盘空间
```bash
df -h
du -sh /home/diary-app/diary-app
```

## 🐛 故障排除

### 常见问题及解决

#### 1. 端口被占用
```bash
# 症状：Error: listen EADDRINUSE :::3000
# 解决：
lsof -ti:3000 | xargs kill -9
# 或使用其他端口
PORT=3001 npm run dev
```

#### 2. 内存不足
```bash
# 症状：JavaScript heap out of memory
# 解决：
export NODE_OPTIONS="--max-old-space-size=4096"
npm run dev
```

#### 3. 权限问题
```bash
# 症状：EACCES permission denied
# 解决：
sudo chown -R $USER:$GROUP /home/diary-app/diary-app
chmod -R 755 /home/diary-app/diary-app
```

#### 4. 依赖问题
```bash
# 症状：Module not found
# 解决：
rm -rf node_modules package-lock.json
npm install
```

## 📝 日常维护检查清单

### 每日检查
- [ ] 应用是否正常启动 (`npm run dev`)
- [ ] 端口占用情况正常
- [ ] 没有JavaScript错误
- [ ] API接口响应正常

### 每周检查
- [ ] 依赖版本更新 (`npm outdated`)
- [ ] 安全漏洞扫描 (`npm audit`)
- [ ] 清理构建缓存
- [ ] 备份重要数据

### 每月检查
- [ ] 性能监控报告
- [ ] 日志文件清理
- [ ] 服务器磁盘空间
- [ ] 更新维护文档

## 🔄 回滚操作

### 回滚到上一个稳定版本
```bash
cd /home/diary-app/diary-app
git checkout HEAD~1
npm install
npm run build
npm start
```

### 清理所有变更并重置
```bash
cd /home/diary-app/diary-app
git reset --hard HEAD
git clean -fd
npm install
npm run dev
```

## 📞 紧急联系

当遇到无法解决的问题时：
1. 保存当前日志
2. 记录错误信息
3. 尝试重启服务
4. 如果问题持续，寻求技术支持

---

**注意**: 执行任何破坏性操作前，请确保已备份重要数据。